// Parser diagnostic regression sample:
// - focuses on parser-coded diagnostics (P1xxx)
// - should trigger helpful notes for missing expression / missing ';'

fun main() {
    var a: Int = ;          // missing expression -> Expected primary expression
    var b: Int = 2          // missing ';' before next statement -> Expected ';'
    if (a > ) {             // missing rhs expression
        var c: Int = 3
    } else {
        var d: Int = 4;;
    }

    foo(1, , 3);            // malformed argument list
    while (a < 10 {         // missing ')'
        a = a + 1;
    }

    return;
}
