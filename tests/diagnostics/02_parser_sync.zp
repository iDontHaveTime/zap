fun main() {
    // parser sync stress: keep syntax mostly legal, but still trigger parser errors

    var a: Int = 1;             // ok
    var b: Int = ;              // missing expression
    var c: Int = 3              // missing semicolon

    if (a > ) {                 // missing rhs expression
        var x: Int = 10;
    } else {
        var y: Int = 20;;
    }

    foo(1, , 3);                // malformed argument list

    while (a < 10 {             // missing ')'
        a = a + 1;
    }

    var s: String = "ok";
    s = s + ;                   // missing rhs expression

    return;
}
